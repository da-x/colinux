The code in this directory is the OS independent kernel code driver.

It is responsible for the following:
 * Monitor - allocating, maintaining and running the coLinux kernel.
 * Manager - manages the running monitors.
